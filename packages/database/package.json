{
  "name": "@url-shortener/database",
  "private": true,
  "version": "0.1.0",
  "description": "Shared SQLite access layer for the URL shortener.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "sql.js": "^1.11.0"
  },
  "devDependencies": {
    "@types/sql.js": "^1.0.4",
    "rimraf": "^5.0.10",
    "typescript": "5.5.4"
  }
}
