{
  "name": "@url-shortener/database",
  "private": true,
  "version": "0.1.0",
  "description": "Shared Prisma schema and client for the URL shortener.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "prisma"
  ],
  "scripts": {
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "studio": "prisma studio",
    "build": "npm run generate && tsc -p tsconfig.json",
    "clean": "rimraf dist prisma/dev.db"
  },
  "dependencies": {
    "@prisma/client": "^6.18.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "prisma": "^6.18.0",
    "rimraf": "^5.0.10",
    "typescript": "5.5.4"
  }
}
